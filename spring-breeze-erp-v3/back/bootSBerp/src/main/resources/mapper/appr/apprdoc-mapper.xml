<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sb.erp.appr.repository.ApprDocMapper">

	<resultMap id="apprDocSummaryMap" type="ApprDocSummaryResponse">
		<id property="docId" column="doc_id"/>
		<result property="empId" column="emp_id"/>
		<result property="docTitle" column="doc_title"/>
		<result property="docStatus" column="doc_status"/>
		<result property="important" column="is_important"/>
		<result property="createdAt" column="created_at"/>
		<result property="empName" column="emp_name"/>
		<result property="linStatus" column="lin_status"/>
	</resultMap>

	<resultMap id="apprDocDetailMap" type="ApprDocResponse">
	    <id property="docId" column="doc_id"/>
	    <result property="forId" column="for_id"/>
	    <result property="forVersion" column="for_version"/>
	    <result property="forTitle" column="for_title"/>
	    <result property="forSchema" column="for_schema"/>
	    <result property="forContent" column="for_content"/>
	    <result property="empId" column="emp_id"/>
	    <result property="empName" column="emp_name"/>
	    <result property="comId" column="com_id"/>
	    <result property="docTitle" column="doc_title"/>
	    <result property="docContent" column="doc_content"/>
	    <result property="docStatus" column="doc_status"/>
	    <result property="important" column="is_important"/>
	    <result property="docRevision" column="doc_revision"/>
	    <result property="createdAt" column="created_at"/>
	    <result property="updatedAt" column="updated_at"/>
	</resultMap>
		
	<!-- 결재 문서 작성하려는 사용자의 회사 양식 가져오기 / 활성화된 양식만 -->
	<select id="findForm" resultType="ApprFormResponse">
		select * 
		from 
			appr_form f
		where
			f.com_id = #{comId}
			and f.for_status = 1
			and f.is_deleted = 0
			and f.for_version = (
				select max(ff.for_version)
				from
					appr_form ff
				where
					ff.com_id = f.com_id
					and ff.for_id = f.for_id
					and ff.for_status = 1
					and ff.is_deleted = 0
			)
	</select>
	
	<!-- 결재 양식 작성하러 들어갈때 사용자 관련 정보 가져오기-->
	<select id="initResponse" resultType="ApprDocInitResponse" >
		select
			e.emp_id,
			e.emp_name,
			p.pos_name,
			p.pos_order,
			c.com_id,
			c.com_name,
			d.dept_id,
			d.dept_name
		from
			employee e
		inner join company c on e.com_id = c.com_id
		inner join department d on e.dept_id = d.dept_id
		inner join emp_position p on e.pos_id = p.pos_id
		where
			e.emp_id = #{empId}
		
	</select>
	
	<!-- 문서 작성 -->
	<insert id="insertDoc">
		insert into appr_doc (
			emp_id,
			for_id,
			for_version,
			com_id,
			doc_title,
			doc_content,
			is_important
		) values (
			#{empId},
			#{req.forId},
			#{req.forVersion},
			#{comId},
			#{req.docTitle},
			#{req.docContent},
			#{req.important}
		)
	</insert>
	
	<!-- 시퀀스 값 가져오기 -->
	<select id="selectCurrentDocSeq" resultType="Long">
		select appr_doc_seq.currval from dual
	</select>

	<!-- 대시보드용 통계 -->
	<select id="selectDocCnt" resultType="map">
		select
			count(distinct case when d.emp_id = #{empId} or l.emp_id = #{empId} then d.doc_id end) as "TOTALCNT",
			count(distinct case when (d.emp_id = #{empId} or l.emp_id = #{empId}) and d.doc_status = 'APP' then d.doc_id end) as "APPCNT",
			count(distinct case when (d.emp_id = #{empId} or l.emp_id = #{empId}) and d.doc_status = 'REJ' then d.doc_id end) as "REJCNT",
			count(distinct case when (d.emp_id = #{empId} or l.emp_id = #{empId}) and d.doc_status = 'ING' then d.doc_id end) as "INGCNT",
			count(distinct case when l.emp_id = #{empId} and d.doc_status = 'ING' and l.lin_status = 'WAI' then d.doc_id end) as "MYCNT"
		from
			appr_doc d
		left join
			appr_line l on d.doc_id = l.doc_id
		where
			d.emp_id = #{empId} or l.emp_id = #{empId}
	</select>
	
	<!-- 모든 문서 조회 -->
	<select id="selectMyHistoryDocs" parameterType="ApprDocSearchCondition" resultMap="apprDocSummaryMap">
		select distinct
			d.doc_id,
			d.doc_title,
			d.emp_id,
			d.doc_status,
			d.is_important,
			d.created_at,
			(select
				emp_name
			 from
			 	employee
			 where
			 	 emp_id = d.emp_id) as emp_name,
			(select
				 lin_status
			 from
			 	 appr_line
			 where
			 	doc_id = d.doc_id and emp_id = #{empId}) as lin_status
		from
			appr_doc d
		where
			(d.emp_id = #{empId}
			or exists (select 1 from appr_line where doc_id = d.doc_id and emp_id = #{empId}))
			<if test="status != null and status != ''">
				and d.doc_status = #{status}
			</if>
			<if test="keyword != null and keyword != ''">
				and (
					d.doc_id like '%' || #{keyword} || '%'
					or d.doc_title like '%' || #{keyword} || '%'
					or (select emp_name from employee where emp_id = d.emp_id) like '%' || #{keyword} || '%'
				)
			</if>
		order by
			d.created_at desc
		offset #{pstartno} rows fetch next #{onepagelist} rows only
	</select>
	
	<!-- 했던 문서 카운트 -->
	<select id="selectMyHistoryDocsCnt" parameterType="ApprDocSearchCondition" resultType="int">
		select count(distinct d.doc_id)
		from
			appr_doc d
		where
			(d.emp_id = #{empId}
			or exists (select 1 from appr_line where doc_id = d.doc_id and emp_id = #{empId}))
			<if test="status != null and status != ''">
				and d.doc_status = #{status}
			</if>
			<if test="keyword != null and keyword != ''">
				and (
					d.doc_id like '%' || #{keyword} || '%'
					or d.doc_title like '%' || #{keyword} || '%'
					or (select emp_name from employee where emp_id = d.emp_id) like '%' || #{keyword} || '%'
				)
			</if>
	</select>
	
	<!-- 내가 결재해야 할 문서 -->
	<select id="selectMyTodoDocs" parameterType="ApprDocSearchCondition" resultMap="apprDocSummaryMap">
		select
			d.doc_id,
			d.doc_title,
			d.emp_id,
			d.doc_status,
			d.is_important,
			d.created_at,
			(select emp_name from employee where emp_id = d.emp_id) as emp_name
		from
			appr_doc d
		inner join
			appr_line l on d.doc_id = l.doc_id
		where
			l.emp_id = #{empId}
			and l.lin_status = 'WAI'
			and d.doc_status = 'ING'
			<if test="keyword != null and keyword != ''">
				and (
					d.doc_id like '%' || #{keyword} || '%'
					or d.doc_title like '%' || #{keyword} || '%'
					or (select emp_name from employee where emp_id = d.emp_id) like '%' || #{keyword} || '%'
				)
			</if>
		order by
			d.created_at desc
		offset #{pstartno} rows fetch next #{onepagelist} rows only
	</select>
	
	<!-- 해야할 문서 카운트 -->
	<select id="selectMyTodoDocsCnt" parameterType="ApprDocSearchCondition" resultType="int">
		select count(*)
		from
			appr_doc d
		inner join
			appr_line l on d.doc_id = l.doc_id
		where
			l.emp_id = #{empId}
			and l.lin_status = 'WAI'
			and d.doc_status = 'ING'
			<if test="keyword != null and keyword != ''">
				and (
					d.doc_id like '%' || #{keyword} || '%'
					or d.doc_title like '%' || #{keyword} || '%'
					or (select emp_name from employee where emp_id = d.emp_id) like '%' || #{keyword} || '%'
				)
			</if>
	</select>
	
	<!-- 기안자 상사들 목록 조회 -->
	<select id="approversByEmpId" resultType="ApprLineResponse">
		select
			e.emp_id,
			e.emp_name,
			p.pos_name
		from
			employee e
		join
			emp_position p on e.pos_id = p.pos_id
		where
			e.dept_id = (select dept_id from employee where emp_id = #{empId})
			and e.emp_id != #{empId}
		order by
			p.pos_order desc
	</select>
	
	<!-- 문서 상태 갱신 -->
	<update id="updateDocStatus">
		update appr_doc set doc_status = #{docStatus} where doc_id = #{docId}
	</update>
	
	<!-- 결재선 지정 상사 조회 -->
	<select id="selectDeptEmpsForLines" resultType="ApprLineResponse">
		select
			e.emp_id,
			e.emp_name,
			e.emp_status,
			p.pos_name,
			p.pos_order
		from
			employee e
		join
			emp_position p on e.pos_id = p.pos_id
		where
			e.dept_id = #{deptId}
			and e.emp_status != '퇴직'
			and e.emp_id != #{empId}
			and p.pos_order > (
				select
					pp.pos_order
				from
					employee ee
				join
					emp_position pp on ee.pos_id = pp.pos_id
				where
					ee.emp_id = #{empId}
			)	
	</select>
	
	<!-- 결재선 지정 가능 인원수 / 사용자 본인 빼고, 본인보다 직급 높은사람들 -->
	<select id="cntApprovers" resultType="int">
		select count(*)
		from
			employee e
		join
			emp_position p on e.pos_id = p.pos_id
		where
			e.dept_id = #{deptId}
			and e.emp_id != #{empId}
			and e.emp_status != '퇴직'
			and p.pos_order > (
				select pp.pos_order
				from employee ee
				join emp_position pp on ee.pos_id = pp.pos_id
				where ee.emp_id = #{empId}
			)
	</select>
	
	<!-- 문서 상세 조회 -->
	<select id="selectDocDetail" resultMap="apprDocDetailMap">
		select
			d.doc_id,
			d.doc_title,
			d.doc_content,
			d.doc_status,
			d.is_important,
			d.created_at,
			d.emp_id,
			f.for_id,
			f.for_version,
			f.for_title,
			f.for_schema,
			f.for_content,
			(select 
				emp_name
			 from
			    employee
			 where emp_id = d.emp_id) as emp_name
		from
			appr_doc d
		inner join
			appr_form f on d.for_id = f.for_id and d.for_version = f.for_version
		where
			doc_id = #{docId}
	</select>
	
</mapper>
